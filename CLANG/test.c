int test(void)
{
	int brst;
	int ocjena;
	float prosjek, suma = 0;
	int min, max;

	scanf("%d", &brst);

	for (int i = 0; i < brst; i++)
	{
		scanf("%d", &ocjena);

		suma += ocjena;

		min = i == 0 ? ocjena : ;
		max = i == 0 ? ocjena
	}
}